# Port yang akan digunakan server
PORT=8080

# Koneksi PostgreSQL (sesuaikan user/password/host/db)
DATABASE_URL=postgres://postgres:postgres@localhost:5432/mealsu

# Secret untuk signing JWT (ganti di production)
JWT_SECRET=dev_secret_change_me
